{
	"name": "ha-floorplan-3d",
	"version": "1.0.0",
	"description": "",
	"scripts": {
		"dev": "npm run dev --workspace client & cd apps/backend && go run .",
		"generate:types": "bash scripts/generate-types.sh"
	},
	"workspaces": [],
	"dependencies": {
		"zod-to-json-schema": "^3.25.0"
	}
}
