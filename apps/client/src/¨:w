import React from "react";
import Modal from "@/components/Modal";

export default function ErrorList({ isOpen, closeModal }) {
  return (
    <Modal isOpen={isOpen} onClose={closeModal}>
      <div className="w-auto h-max-auto bg-yellow-500"></div>
    </Modal>
  );
}
