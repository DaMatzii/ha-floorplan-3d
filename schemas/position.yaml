$id: https://example.com/schemas/action.json
$schema: http://json-schema.org/draft-07/schema#
title: Position
type: object
description: A single entity configuration within a room.
definitions:
  position:
    title: Position
    type: object
    description: The x, y, z position of the entity on the floorplan.
    properties:
      x:
        type: number
      y:
        type: number
      z:
        type: number
    required:
      - x
      - y
      - z
    additionalProperties: false
